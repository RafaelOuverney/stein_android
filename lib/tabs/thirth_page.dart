import 'package:flutter/material.dart';

class ThirthTab extends StatelessWidget {
  const ThirthTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
      child: const Center(
        child: Text('Tela 3'),
      ),
    );
  }
}
